/*const Adicionar =  require('../models/adicionarModel');
const adicionar = [];

async function addAdicionar(req, res) { 
	const { id, title } = req.body; 
	const adicionar = new Adicionar(id, title, false); 
	await adicionar.salvar();

	res.redirect('/adicionar'); 
} 

module.exports = {addAdicionar};*/